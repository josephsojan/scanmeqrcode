import React from 'react';

export function Hero() {
    return (
        <section className="hero">
            <p className="eyebrow">01 — Paste a link, 02 — get a code</p>
            <h1>Turn any link into a code you can scan in a blink.</h1>
            <p>Everything renders directly in your browser — zero tracking, zero server logs. Enter text, tune error correction and colors, then export your vector or PNG.</p>
        </section>
    );
}
