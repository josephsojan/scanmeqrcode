import React from 'react';

export function Hero() {
    return (
        <section className="hero">
            <p className="eyebrow">01 — Paste a link, 02 — get a code</p>
            <h1>Turn URLs  , vCards, and text into high-quality, custom QR codes.</h1>
        </section>
    );
}
